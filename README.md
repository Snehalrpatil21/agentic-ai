# RAG-Langchain-Chatbot Demo Project

## Install dependencies
1. pip3 install 'onnxruntime'
2. Install all the dependancies mentioned in 'requirements.txt'.
   pip3 install -r requirements.txt
3. Install markdown depenendies:
   pip3 install "unstructured[md]"


## Create database

Create the Chroma DB.

Using - python create_database.py

## Query the database


